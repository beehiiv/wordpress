/**
 * Beehiiv mark for the plugin sidebar (mask + currentColor, like admin menu icon).
 */
export default function BeehiivSidebarIcon() {
	return <span className="beehiiv-sidebar-icon" aria-hidden="true" />;
}
